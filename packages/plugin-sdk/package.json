{
  "name": "@explorer-engine/plugin-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "Stable, framework-agnostic API for authoring plugins (a pure re-export façade over @explorer-engine/core's plugin contract — no business logic). The ONLY dependency a plugin may take.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@explorer-engine/core": "1.0.0"
  }
}
