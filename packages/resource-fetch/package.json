{
  "name": "@explorer-engine/resource-fetch",
  "version": "1.0.0",
  "private": true,
  "description": "ResourceTransport adapter — the ONLY resource package allowed to use fetch/AbortController. Performs the HTTP request, converts the response to bytes, returns URL + MIME, maps cancellation onto AbortController. No GLB/textures/audio/ObjectURL.",
  "license": "UNLICENSED",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@explorer-engine/core": "1.0.0"
  }
}
