{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // This is an application (not the headless core): DOM is allowed here.
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["vite/client"],
    "noEmit": true
  },
  "include": ["src"]
}
