# examples/explorer-packages/

Explorer Packages d'exemple (**données uniquement** : GLB + `config.json` + assets). Aucun code moteur ne réside ici — c'est la preuve continue de la séparation moteur/contenu (ENGINE_CONSTITUTION L1/L2/L29).

- **Statut** : conteneur vide (P0-T1). Aucun package d'exemple n'est créé à ce stade (voir P10).
- **Note** : ce dossier n'est **pas** un workspace npm (données, pas de `package.json`).
- **Référence** : chapitres 03 §3.5, 04.
